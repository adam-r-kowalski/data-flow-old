{
    "name": "data-flow",
    "version": "1.0.0",
    "license": "MIT",
    "devDependencies": {
        "@types/jest": "^27.5.1",
        "@types/papaparse": "^5.3.3",
        "buffer": "^6.0.3",
        "gh-pages": "^3.2.3",
        "jest": "^28.1.0",
        "process": "^0.11.10",
        "rimraf": "^3.0.2",
        "ts-jest": "^28.0.3",
        "typescript": "^4.7.2",
        "vite": "^3.1.6"
    },
    "dependencies": {
        "@tensorflow/tfjs": "^3.21.0",
        "papaparse": "^5.3.2"
    },
    "scripts": {
        "dev": "yarn vite",
        "preview": "yarn vite preview --port 5173",
        "build": "yarn vite build",
        "prebuild": "yarn rimraf dist",
        "push": "yarn gh-pages -d dist",
        "prepush": "yarn build",
        "test": "yarn jest",
        "coverage": "yarn jest --coverage"
    }
}
