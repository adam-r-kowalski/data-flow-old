export interface Size {
    width: number
    height: number
}

export interface Constraints {
    minWidth: number
    maxWidth: number
    minHeight: number
    maxHeight: number
}

export interface Layout {
    size: Size
}